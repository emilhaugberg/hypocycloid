{
  "name": "dots-in-circle",
  "version": "1.0.0",
  "description": "Kewl animation.",
  "main": "index.js",
  "scripts": {
    "build": "export PATH=node_modules/.bin:$PATH && node_modules/.bin/pulp build && webpack",
    "start": "npm run build && webpack-dev-server --content-base public/ --hot --inline"
  },
  "author": "Emil van Veen <emilveen@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "pulp": "^10.0.0",
    "purescript": "^0.10.5"
  },
  "devDependencies": {
    "webpack": "^1.14.0"
  }
}
